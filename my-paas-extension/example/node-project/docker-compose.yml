# Auto-generated by My PaaS Extension
# Project: my-project

services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      NODE_ENV: production
    volumes:
      - .mypaas/services/app/logs:/var/log/app
    restart: unless-stopped
